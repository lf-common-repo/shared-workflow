name: Sleep Mode Workflow

on:
  workflow_dispatch:
    #inputs:
     # manual_trigger:
      #  description: 'Manually trigger the workflow'
       # required: true
        #default: 'false'

#env:
 # JAVA_VERSION: '17'

jobs:
  dummy1:
    runs-on: self-hosted
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@v3

  #dummy2:
   # runs-on: self-hosted
    #if: ${{ inputs.manual_trigger == 'true' }}
    #steps:
     # - name: 'Checkout GitHub Action'
        uses: actions/checkout@v3
